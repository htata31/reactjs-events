# reactjs-events
Steps to Start the Application
1. Execute npm install which installs the packages required from the package.json
2. Execute the npm start command to start the server and load the application in the browser
3. Excute npm run test for eunning the unit test cases of the application

Application commponent structures is 
Parent component - App 
Child components - Homepage and Navbar